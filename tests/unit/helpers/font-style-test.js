import { fontStyle } from 'digital-mixtape/helpers/font-style';
import { module, test } from 'qunit';

module('Unit | Helper | font style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fontStyle([42]);
  assert.ok(result);
});
