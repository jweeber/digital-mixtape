import { fontColor } from 'digital-mixtape/helpers/font-color';
import { module, test } from 'qunit';

module('Unit | Helper | font color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fontColor([42]);
  assert.ok(result);
});
